import { colors } from "./color";

const themes = {
  default: {
    main: colors.main,
    secundary: colors.teal100
  },
  light: {
    main: colors.white,
    secundary: colors.main
  }
}

export {themes};